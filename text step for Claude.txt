Which phases should I tackle now for the closed-form T-matrix representation?

❯ 1. Phase A only (stiffness-extended Section 10b)
     Extend Section 10 to include B_el. Keeps atoms opaque. Gets you closed-form rational functions in
     ~seconds, verified against Section 12b. Lets us commit + discuss before deeper work.
  2. Phases A + B (replace T_9 atoms with Mp closed forms)
     Also substitute symbolic Mp-based closed forms for alpha9/beta9/gamma9/b11const. May blow up LeafCount
     for the 4x4 T_1u block (already 20M leaves with opaque atoms).
  3. Phases A + B + D (skip C, typeset what we have)
     Do phases A and B, defer the YY scalar PSLQ (phase C), typeset the resulting gerade-irrep expressions in
     the paper with any remaining atoms kept symbolic.
  4. All four phases
     Full closed form down to elementary constants, including PSLQ of YY scalars. This is the most work and
     the YY PSLQ may fail or take many hours of Mathematica time.
